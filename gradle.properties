org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false
systemProp.org.gradle.internal.http.socketTimeout=120000
systemProp.org.gradle.internal.http.connectionTimeout=120000

# The Minecraft version must agree with the NeoForge version to get a valid artifact
minecraft_version=1.21.1
# The Minecraft version range can use any release version of Minecraft as bounds.
minecraft_version_range=[1.21.1,1.21.2)
# The NeoForge version must agree with the Minecraft version to get a valid artifact
# 构建版本沿用工作区其它 1.21.1 项目的 21.1.248（复用 .gradle-home 的 neoForm 缓存，构建更快）
neo_version=21.1.248
# The NeoForge version range can use any version of NeoForge as bounds or match the loader version range
# 沿用 MI 的最低 NeoForge 需求（21.1.219），兼容更多版本（AE2 最低 21.1.169 更低）
neo_version_range=[21.1.219,22-)
# The loader version range can only use the major version of NeoForge/FML as bounds
loader_version_range=[2,)
# The mapping channel to use for mappings.
mapping_channel=official
mapping_version=1.21.1

# The unique mod identifier for the mod. Must be lowercase in English locale.
mod_id=mi_ae2_pattern_provider
# The human-readable display name for the mod.
mod_name=MI AE2 Pattern Provider
# The license of the mod.
mod_license=MIT
# The mod version. See https://semver.org/
mod_version=1.0.0
# The group ID for the mod.
mod_group_id=com.miae2
# The authors of the mod.
mod_authors=EasyRain
# The description of the mod.
mod_description=An Applied Energistics 2 pattern provider hatch for Extended Industrialization's Processing Array.
